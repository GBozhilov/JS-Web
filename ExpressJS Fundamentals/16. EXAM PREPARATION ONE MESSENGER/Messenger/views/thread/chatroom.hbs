<h1>Chatroom with {{username}}</h1>
{{#if isOtherBlocked}}
    <form method="post" class="chat-form" action="/user/unblock/{{username}}">
        <button class="btn btn-success">Unblock</button>
    </form>
{{else}}
    <form method="post" class="chat-form" action="/user/block/{{username}}">
        <button class="btn btn-danger">Block</button>
    </form>
{{/if}}
<div class="chat-wrapper">
    <div id="message-wrapper">
        {{#each messages}}
            <div class="message-row">
                {{#if isImage}}
                    {{#if isLeft}}
                        <img class="thread-message left" src="{{content}}">
                    {{else}}
                        <img class="thread-message right" src="{{content}}">
                    {{/if}}
                {{else}}
                    {{#if isLeft}}
                        <span class="thread-message left">{{content}}</span>
                    {{else}}
                        <span class="thread-message right">{{content}}</span>
                    {{/if}}
                {{/if}}
            </div>
        {{/each}}
    </div>

    {{#if amIBlocked}}
        <h2 class="error">{{username}} blocks you</h2>
    {{else}}
        <form method="post" class="chat-form">
            <input value="{{threadId}}" name="threadId" hidden/>
            <textarea rows="5" name="content" placeholder="message"></textarea>
            <button class="btn btn-primary send">Send</button>
        </form>
    {{/if}}
</div>